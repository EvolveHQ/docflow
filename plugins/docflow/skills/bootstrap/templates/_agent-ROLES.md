# Agent Roles

**Authoritative for:** which writer owns which artefacts. Nothing else
in this repo records the division of work, so this file is the contract.
It is not a log — it records who writes what, never what was written.

<!-- Written in shared-checkout and separate-worktree modes only. A
single-writer repo has no roles to divide and gets no ROLES.md. -->

## <agent-id>

<What this writer owns — the ADRs, areas, or paths it is the single
writer of. One section per writer.>

<!-- Example, for a repo that splits by area:

## frontend-agent
The frontend agent owns UI, components, design tokens, and front-end
build pipeline ADRs.

## backend-agent
The backend agent owns API surface, data model, persistence, and
backend infrastructure ADRs.

## docs-agent
The docs agent owns README, GLOSSARY, INDEX regeneration, and external
documentation outputs.
-->
