# Native Cowork continuation — 2026-09-14

**Cowork now has actual-target signing, lifecycle and delegated-wave evidence.**
Desktop 1.52386.6 / the visible Opus 5 Max selection executed through a Linux
VM/FUSE mount of the operator-selected Windows target. Controller UI observations
and independent Windows filesystem/Git checks are separated in the
[sanitised receipt](../evals/hosts/results/2026-09-14-cowork.json). No cloud mirror
or substitute host supplies this result.

The original bootstrap/new-adr passed 23 existing assertions plus six wrapper
checks. An explicitly opted-in native Workflow ran two executors with assigned
Git worktrees. Forty-seven state assertions and four recovery/output checks
confirm signed local claims and recoverable work, unchanged main and held claim,
and no integration after the environmental gate failure. Original child tool
events establish successful create-only acquisition before each first output
write, and both writes started before alpha's failure. The peer was already
running and finished; no new item started. Twenty bounded receipt checks include
the discovery of a wrong beta timestamp in the native manifest, not twenty
additional acceptance passes. The exact instant the parent learnt of the failure
is unavailable in the exported child history.

Extra inspection found a **generic, unresolvable seed completion footer** which
the original assertions missed. Separate signed commits add a stricter resolver
and then clarify bootstrap/templates: the seed alone may name its exact
introduction path and resolver, verified after the atomic scaffold commit.
Ordinary completion still names existing verified work. Fourteen positive/negative
regressions pass. A fresh native bootstrap using repaired source `eff3130`
passed 25 independent checks; its reference resolves to signed full-scaffold
commit `48dcf4ed70db152bb5b375a5ea9d59f512996bb0`. The original failure is retained.

Both tested installations matched all 32 exported files and nine sidecars.
Original lifecycle/wave evidence uses `13ea0c2`; the repair received a targeted
bootstrap rerun on `eff3130`. Windows-export bytes differ from Git blobs by CRLF
serialisation; the receipt records per-file hashes and each aggregate algorithm.
This does not establish a new five-host matrix on the repaired bootstrap.

Permission limits remain material. **Skip approvals was already enabled** and
was not changed. Initial Git lock unlink/staging failed, then recovered through
the supported target-scoped deletion grant. Native `gh` is absent; synthetic SSH
signing and pushes prove only local bare-remote transport. In a separate
post-wave export, the file API denied seven `.git` destinations; Cowork copied
through another tool before the controller's stop completed. That boundary
violation and the unsuitable initial export destination remain adverse findings.
Later inventory used an ordinary target path. The earlier cloud denial, marker
correction, autosaved-output discrepancy and corrected public-key transcription
are preserved separately.

The worker performed no GUI actions, native target writes, Pi inference or
container mutations. Owned upload copies and the native synthetic private-key
file were removed. Cleanup's assumed timestamp and agent-state overclaims were
corrected; broader agent state remains unverified. Public verification material
and all recovery evidence are retained for review. Version
0.9.4 remains synchronised. Validation and exact-head CI are tracked on
[draft PR #6](https://github.com/EvolveHQ/docflow/pull/6); nothing is merged or
released. ADRs 0012/0015 remain Accepted and 0051 remains todo.

## Status at a glance

- **This run:** verified native Cowork phases and the targeted bootstrap repair;
  preserved permission, reporting and original test failures.
  Validation: verify OK, 15 mutation rejections, 14 seed tests passed,
  deterministic 6 passed / 0 failed / 6 skipped; all commands exited 0.
- **Overall:** bounded continuation verified; full five-host release incomplete.
- **Yet to do:** successful native-high Pi full-contract evidence and remaining
  release review; retain Cowork's permission/source limits, unverified broader
  agent state and untested hosted PR
  capability. No further desktop attachment or login is needed for these results.
