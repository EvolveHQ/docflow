Use the installed Docflow workspace-plan skill in /workspace/recommendations. Native actor is test:recommendation-planner, owner is test:scenario-controller, host is Claude Code. This is a disposable test workspace. The controller authorises scoped canonical planning and derived index edits inside this workspace only, with read-only access to its declared member repositories; no implementation, member edits, grant/run creation, claim acquisition, package installation, remote repository operations, account changes or delegation. Stop after the planning result and local consistency check. Follow the workspace's own instructions. Record current UTC, full record references, source paths/revisions and actual check commands/exits. Report the native advertised Docflow skills and the actual loaded skill/asset paths. All fixture histories and source catalogues are synthetic test inputs, not prior runtime proof.

Refine the three existing planned reviews: the contributor handbook navigation, the React search input accessibility, and the Svelte/Tauri folder bridge boundary. Inspect the actual member files, source catalogue, current user choices and DATA-POLICY.md. For each, record a justified approach and any applicable guidance/tools, availability, constraints, dependencies, fallback and a check that would establish a useful review. The goal is useful review planning; do not perform the implementation or reviews themselves. Keep the current owners and the existing scope. Use only sources available in the declared workspace. I have already declined optional retrieval for the React review because direct file inspection suffices; that choice and its context are in the existing record. No optional setup is authorised.