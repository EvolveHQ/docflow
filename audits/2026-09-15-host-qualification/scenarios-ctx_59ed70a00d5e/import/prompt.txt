Use the installed Docflow workspace-plan skill in /workspace/import. Native actor is test:import-planner, owner is test:scenario-controller, host is Claude Code. This is a disposable test workspace. The controller authorises scoped canonical planning and derived index edits inside this workspace only, with read-only access to its declared member repositories; no implementation, member edits, grant/run creation, claim acquisition, package installation, remote repository operations, account changes or delegation. Stop after the planning result and local consistency check. Follow the workspace's own instructions. Record current UTC, full record references, source paths/revisions and actual check commands/exits. Report the native advertised Docflow skills and the actual loaded skill/asset paths. All fixture histories and source catalogues are synthetic test inputs, not prior runtime proof.

Import the two unpublished backlog idea proposals under .docflow_workspace/local/incoming into this canonical workspace. One proposal came from an independent native planner; the contact proposal is a deliberately injected input fixture with its own assigned identity. Preserve the identities and original creation histories supplied by those proposals, and preserve established canonical links. Existing canonical records are already published within this test home. Do not create agreements, work or execution records. Stop with the import result or a precise blocker.