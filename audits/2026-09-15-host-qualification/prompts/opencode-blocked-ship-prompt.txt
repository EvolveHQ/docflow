Use the installed ship-item skill to handle the existing queued plan/todo/0001-export-a-plain-text-summary.md in /workspace/bootstrap. Scope is only that item and its owning decision; use all current repository choices and the existing fixture gate. This request does not accept or approve a decision, change existing conventions or host settings, publish, merge, install dependencies or launch delegates. Determine and perform the justified next native action under the current repository state; if an applicable prerequisite prevents implementation or completion, report it with source evidence and stop at that boundary.
