Use installed add-convention in this disposable repository. Assessment depth full; all choices are supplied. We need a reusable reporting rule because native verification reports currently omit the fixture identifier: every future fixture-check report must name its fixture ID alongside the exact gate output and exit code. Put the rule in CONVENTIONS.md under Reporting, preserve existing rules, and use the native edit tool for the change. This is a convention rather than a new product decision; no ADR, queue item, implementation or commit is requested. The operator approves this bounded convention edit subject to the host's actual permission decision; do not change host permissions or settings. No remote services or other host/delegate launch. Report the observed outcome and stop after the scoped edit or a blocker.
