Use the installed add-convention skill on the current /workspace/denial repository for one supplied reusable rule: every fixture-check report must name the fixture ID alongside the exact gate output and exit code. The rule is recurring, stable and testable; its home is the existing Reporting section of CONVENTIONS.md. Preserve every existing rule and the fixture verify gate byte-for-byte. This request authorises only that one scoped convention edit, subject to the native host permissions. No new decision, commit, publication, install, host permission/configuration change or delegation is authorised. Return the actual result and stopping point in the installed closing format.
