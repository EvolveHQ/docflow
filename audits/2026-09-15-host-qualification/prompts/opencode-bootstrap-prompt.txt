Use the installed Docflow bootstrap skill in /workspace/bootstrap, then installed new-adr. This is a real disposable repository qualification, and all material choices are supplied and approved for this run.
Bootstrap depth full; project name Native qualification fixture; purpose test documentation-led workflows; artefact root .; language en-GB; one capability ADR shape; include plan queue and seed adoption decision; single writer; direct integration into local main using fast-forward, no remote push; enable autonomous prompt with existing gate node tools/verify.mjs; no glossary, federation, domains, ROLES, ADR-revision tags or coauthor trailer; Conventional Commits, mandatory Rationale footer on ADR changes; unsigned fixture commits are explicitly selected. Preserve the supplied gate byte for byte and all unrelated files. You have authority for local fixture edits, gates and commits only, including the bootstrap adoption acceptance and completion under those choices. Do not ask again for these answered choices or sign-off.
After bootstrap, author Proposed decision 0002, title Export a plain text summary. Assessment depth full and all scope answers supplied: export input records as ordered lines; acceptance criteria exactly three: (1) output preserves input order as one record per line, (2) empty input yields an empty string, (3) Unicode characters are preserved. No new runtime implementation is requested; output format is plain text, no sorting, formatting options, UI or network. Keep the decision Proposed; update INDEX and commit under the established fixture rules, with no Co-Authored-By trailer. Do not push or use hosted GitHub; no other host or delegate launch. Run the unchanged local gate, preserve a clean committed repository and return exact outcomes. Stop after the Proposed decision commit and final report.
